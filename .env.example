# Phase 2 (Supabase). Copy to ".env" and fill in from your Supabase project:
# Settings → API Keys. Leave unset to keep running the localStorage demo.
# Vite only exposes vars prefixed with VITE_.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
